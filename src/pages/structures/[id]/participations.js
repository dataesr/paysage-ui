import { Text } from '@dataesr/react-dsfr';

import useHashScroll from '../../../hooks/useHashScroll';

export default function StructureParticipationsPage() {
  useHashScroll();

  return (
    <Text>
      Participations
    </Text>
  );
}
